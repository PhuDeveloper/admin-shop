import { ReactNode } from 'react';

export interface DataTypeOrderList {
  key: string;
  stt: ReactNode;
  customer: ReactNode;
  created: ReactNode;
  totalPrice: ReactNode;
  status: ReactNode;
  action: ReactNode;
}
