import { ReactNode } from 'react';

export interface DataTypeUserList {
  key: string;
  stt: ReactNode;
  fullName: ReactNode;
  email: ReactNode;
  phone: ReactNode;
  address: ReactNode;
  role: ReactNode;
  action: ReactNode;
}
